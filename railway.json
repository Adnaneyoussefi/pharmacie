{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "bash -c 'until php bin/console doctrine:migrations:migrate --no-interaction --allow-no-migration; do echo Waiting for DB...; sleep 3; done && php -S 0.0.0.0:${PORT} -t public'",
    "restartPolicyType": "ON_FAILURE"
  }
}