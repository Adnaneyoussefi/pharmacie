{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "until php bin/console doctrine:migrations:migrate --no-interaction --allow-no-migration; do echo 'Waiting for DB...'; sleep 3; done && apache2-foreground",
    "restartPolicyType": "ON_FAILURE"
  }
}