{% extends 'client/base.html.twig' %}



{% block nav %}{% endblock %}
{% block body %}
 <div class="site-section">
     
 
        
           
           <div class="container">
          <div class="row">
          {% for categorie in categories %}
           <div class="col-sm col-lg-2 mt-2" >
          <div class="card">
            <div class="card-body text-center item mb-4">
              <a href="{{path("cat",{"name":categorie.nom})}}">{{categorie.nom}}</a> &nbsp;
            </div>
          </div>
        </div>
        {% endfor %}



{% endblock %}