{% extends 'proprietaire/base.html.twig' %}

{% block title %}Produit{% endblock %}

{% block body %}

<div class="container">
        <div class="row">
          <div class="col-md-5 mr-auto">
            <div class="border text-center">
              <img src={{ asset('images/' ~ stock.produit.image) }} alt="Image" class="img-fluid p-5">
            </div>
          </div>
          <div class="col-md-6">
            <h2 class="text-black">{{ stock.produit.nom }}</h2>
            <p>{{ stock.produit.description }}</p>
            
            <p>Prix HT :<strong class="text-primary h4">{{ stock.produit.prixHt }} dh</strong></p>
            <p>Prix TVA :<strong class="text-primary h4">{{ stock.produit.prixTva }} dh</strong></p>
            <p>Date d'expiration :<strong class="text-primary h4">{{ stock.produit.dateExpiration ? stock.produit.dateExpiration|date('Y-m-d') : '' }}</strong></p>
            <p>Quantité :<strong class="text-primary h4">{{ stock.quantite }}</strong></p>
            
            
          </div>
          
        </div>

        
        <a href="{{ path('stock_proprietaire') }}" class="btn btn-outline-secondary btn-sm" style="width: 135px">Retour à la liste</a>

        <a href="{{ path('produit_edit', {'id': stock.produit.id}) }}" class="btn btn-warning btn-sm" style="width: 100px">Modifier</a>

        {{ include('proprietaire/_delete_form.html.twig') }}
        
      </div>
{% endblock %}
